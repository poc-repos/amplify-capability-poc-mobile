import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'amplify.capability.poc.mobile',
  appName: 'amplify-capability-poc-mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
