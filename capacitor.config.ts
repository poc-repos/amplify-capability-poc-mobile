import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'amplify-capability-poc-mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
